# grep: Inverse Filter Lines

To filter lines out inversely, use the `-v` option of the `grep` command:

```console
grep -v foo filename.txt
```

This command will output the lines not containing "foo".
